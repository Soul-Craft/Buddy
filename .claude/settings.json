{
  "permissions": {
    "allow": [
      "Bash(*run-buddy-patcher*)",
      "Bash(swift build*)",
      "Bash(*readlink ~/.local/bin/claude*)",
      "Bash(*plutil*claude.json*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'BuddyPatcher'; then echo '⚠️ BYTE-LENGTH INVARIANT: Every binary patch MUST produce output identical in byte length to the original. Verify all assert checks and test with --dry-run before committing.'; fi"
          }
        ]
      }
    ]
  }
}
