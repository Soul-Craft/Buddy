* text=auto

# Swift source — enable function-level diffs
*.swift text diff=swift

# Shell scripts must have LF line endings
*.sh text eol=lf

# Text files
*.md text
*.json text
*.yml text
*.yaml text

# Makefile must use tabs
Makefile text eol=lf
